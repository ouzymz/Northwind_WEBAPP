@model List<Northwind_WEBAPP.DTOs.OrderDTO>

@{
    ViewData["Title"] = "RequestCustormerOrders";
}

<h1>RequestCustormerOrders</h1>

<div>
    <h4>OrderDTO</h4>
    <hr />
    <dl class="row">

        @foreach (var item in Model)
        {
            <dt class="col-sm-2">
                OrderId:
            </dt>
            <dd class="col-sm-10">
                @item.OrderId
            </dd>
            <dt class="col-sm-2">
                CustomerId:
            </dt>
            <dd class="col-sm-10">
                @item.CustomerId
            </dd>
            <dt class="col-sm-2">
                Order Date:
            </dt>
            <dd class="col-sm-10">
                @item.OrderDate
            </dd>
        }

    </dl>
</div>
<div>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>
